import math
n = int(input("Enter the number of integers: "))
not_square = -1
for i in range(n):
    num = int(input(f"Enter integer {i+1}: "))
    sqrt_num = math.isqrt(num)
    if sqrt_num * sqrt_num != num and num > not_square:
        not_square = num
if not_square == -1:
    print("There is no number that is not a perfect square.")
else:
    print("The largest number that is not a perfect square is:",not_psquare)
